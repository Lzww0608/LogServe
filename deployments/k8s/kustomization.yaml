# Minimal local kustomize bundle for kind or other developer clusters. Resource
# ordering keeps namespace/secret creation before workload manifests.
resources:
  - namespace.yaml
  - logd.yaml
  - control.yaml
  - worker.yaml
  - network-policy.yaml
